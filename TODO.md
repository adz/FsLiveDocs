# TODO

- Capture the full docs source model, not just `PackageModel`, in `.livedocs/history/{version}.json` so past versions can rerender with the latest templates and CSS.
- Include top-level pages, guide pages, frontmatter, and page ordering in the version snapshot.
- Keep the renderer current at build time instead of freezing rendered HTML in history artifacts.
