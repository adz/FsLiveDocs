## FsLiveDocs 0.3.3

Fix `capture` rejecting a package that documents a type abbreviation of another documented type.

### Fixes

- Fixed duplicate member IDs when capturing documentation containing a type abbreviation of another
  documented type. The abbreviation still gets its own page without repeating the aliased type's
  members.

### Documentation

https://adz.github.io/FsLiveDocs/
