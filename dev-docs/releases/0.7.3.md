## FsLiveDocs 0.7.3

### Fixed

- Documentation-set history builds preserve an authored set homepage. The renderer no longer generates a fallback `index.html` while the authored index is still pending in the same write batch, which previously replaced custom homepages in versioned sites.
