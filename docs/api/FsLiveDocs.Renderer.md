# FsLiveDocs.Renderer

`FsLiveDocs.Renderer` owns generated presentation.

It converts structured documentation, canonical Markdown, semantic tokens, and site configuration into HTML, CSS, and JavaScript.

The renderer resolves symbol IDs into current API page URLs and validates external links before creating markup.

Renderer changes apply to current and historical capsules without changing persisted release meaning.
